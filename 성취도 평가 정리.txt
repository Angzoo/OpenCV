1. 컴파일 : 고급 언어(java, c언어, python 등)에서 저급 언어(기계어, 어셈블리어)로 번역하는 과정이다.
2. 링크 : 컴파일러의 결과물인 Object code들을 최종 실행 가능한 실행파일(.exe) 만들기 위해 연결 ,병합 해주는 작업이다.
=> 링크 오류는 대부분 라이브러리에서 일어난다.
3. RGB : 8바이트, 빛의 삼원색을 이용, 픽셀정보가 담겨져 있다.
4. YCbCr : jpeg를 저장할때 사용
5. 비트맵 이미지 특징 : 윈도우에 기본 이미지 형식, 상대적으로 용량이 클 수도 있다
6. 이미지(정지영상)과 동영상은 어떤 관계가 있는지, 영상을 처리하는 관점에서 어떤 특징이 있는지 자유롭게 기술하시오.
7. 환경 변수 : 운영체제가 참조하는 변수
8. PATH : 프로그램을 실행할 때 이 실행파일이 어떤 경로에 있는지 순서대로 나열한 변수
9. Mat클래스 특징 : 
10. depth는 하나의 원소가 갖는 원소 타입, channel은 흑백이면 1채널, 컬러는 3채널
11. Release모드는 개발 컴파일을 할 때 Debug모드와 링크하거나 참조할 때 라이브러리가 서로 다르다.
12. waitKey()함수 특징 : 사용자의 키입력을 받고, 결과값은 아스키값이다. 0을 주면 무한대기상태이다.
13. 얕은 복사 :  대입연산자 '=' 에 의해 이루어지는 복사
하나의 이미지를 공유하는 서로 다른 이름의 변수가 생성된 것
14. 깊은 복사 : clone()함수 사용
15. OpenCV에서 사용하는 Mat 클래스를 이용하여 객체를 만들 때, 
64x64 크기의 그레이스케일 검정색 이미지에 해당하는 객체를 만드는 방법을 제시하시오.
16. fps : 
17. namedWindow : 미리 창을 만든다. namedWindow - imshow
18. imread, imwrite
19. Videocapture
20. 명령행 파라미터
21. argc 파라미터 수, argv 파라미터
22. hconcat : OpenCV에서 2개의 이미지를 가로(수평)로 붙혀서 하나의 이미지로 만드는 함수
23. vconcat : OpenCV에서 2개의 이미지를 세로(수직)로 붙혀서 하나의 이미지로 만드는 함수
24. 이진화(threshold()) : 
25. 콜백함수 : 다른함수의 파라미터를 사용, 불림을 당하는 함수
26. 필터(행렬)이 이미지에 적용되는 방식 : 
27. 문자열안에 \(역 슬래시)는 확장 문자열이므로 파일 경로등을 포함할 때 \\ 2개 사용해야 한다.
28. ROI = 관심영역
29. 지역변수 : 하나의 지역안에서만 유효한 변수로 자신이 선언된 함수 안에서만 사용가능
30. 전역변수(global variable) : 모든 전 지역에서 사용할 수 있는 변수, 함수 바깥에서 선언하고, 함수 내부에서도 해당 변수를 사용
31. 파이썬에서 opencv를 이용하기 위해 사용하는 다음 구문의 괄호(           )에 포함될 키워드는 무엇인가?
       (즉, C언어의 #include에 해당하는 파이썬 구문을 물어보는 것입니다.)       (      import      ) cv2
32. 파이썬에서 패키지(라이브러리)를 설치하기 위해 사용하는 명령어 : pip install
33. 템플릿 매칭 : 대상이미지안에서 내가 관심있는 이미지(템플릿)이 있는지 매칭하는 것
34. 다음과 같은 C언어의 함수를 파이썬으로 작성하시오. (즉, 함수를 정의하는 방법을 물어보는 것입니다.)
int add(int a, int b)
{
     int sum = a + b;
     return sum;
}
def add(){
	
}
35. YOLO의 특징 : 영상 내에서 존재하는 객체가 무엇인지와 위치를 나타내는 것
36. 모폴로지 : '형태학'이라는 뜻으로, 영상 처리에서 노이즈 제거, 구멍 채우기, 끊어진 선 이어 붙이기 등에 
쓰이는 형태학적 연산을 말합니다. 
모폴로지 연산은 검은색과 흰색으로만 구성되어 있는 바이너리(binary) 이미지에 적용할 수 있습니다. 
모폴로지 연산으로는 침식, 팽창, 열림, 닫힘이 있다.
37. 히스토그램 : 이미지 내에서 색상의 분포도를 보여주는 것. 
38. 히스토그램 평활화 : 히스토그램이 그레이스케일 전체 구간에서 균일한 분포로 나타나도록 변경하는 명암비 향상 기법
39. 컨볼루션 : resizing 등을 활용하는것
40. OpenCV의 findContours()함수를 이용하여 추출할 수 있는 컨투어의 개념
  => findContours()함수를 이용하여 이미지의 컨투어 정보, 컨투어의 상하 구조 정보를 출력하낟. 흑백이미지 또는 이진화된 이미지만 적용가능하다.
41. 윈도우의 CMD 창에서 명령을 실행하니 명령을 찾을 수 없다고 한다. 이때 점검해야 할 환경변수는 무엇인가? PATH
42. OpenCV에서 임계값을 이용한 이진화를 수행할 때 사용할 수 있는 함수(이름)을 제시하시오. threshold()
43. OpenCV의 waitKey()에 동영상의 FPS를 반영하는 방식을 설명하시오. 
(즉, 동영상을 재생속도에 맞추어 재생하기 위하여 waitKey()를 어떻게 사용해야 하는지 설명하시오.)
  => OpenCV의 waitKey() 함수는 키 이벤트를 기다리는 역할을 하며, 
      동영상의 FPS(Frame Per Second, 초당 프레임 수)에 맞추어 재생하기 위해서는 waitKey() 함수에 파라미터로 1/FPS











